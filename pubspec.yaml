name: http_dev_server
description: "Тестовый HTTP сервер"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.4.0+6

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.6
  logger: ^2.4.0
  package_info_plus: ^8.0.2
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
